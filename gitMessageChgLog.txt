Added Boss Class:  
- checkHealth function for game over condition  
- Every projectile created has hitpoints. Damage read and updated by Boss class.
- In scene object, simply check for collision and if it has collided then set bossObject.projectileHit to true.